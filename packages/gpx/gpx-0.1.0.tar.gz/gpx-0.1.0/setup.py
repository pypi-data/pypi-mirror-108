#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['gpx']

package_data = \
{'': ['*']}

install_requires = \
['lxml >= 4.6.3', 'python-dateutil >= 2.8.1']

extras_require = \
{'dev': ['black >= 20.8b1',
         'coverage >= 5.5',
         'flake8 >= 3.9.1',
         'flake8-bugbear >= 21.4.3',
         'flake8-docstrings >= 1.6.0',
         'mccabe >= 0.6.1',
         'mypy >= 0.812',
         'pytest >= 6.2.3',
         'tox >= 3.23.0']}

setup(name='gpx',
      version='0.1.0',
      description='PyGPX is a python package that brings support for reading, writing and converting GPX files.',
      author='Steven van de Graaf',
      author_email='steven@vandegraaf.xyz',
      url='https://github.com/sgraaf/PyGPX',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='>=3.8',
     )
