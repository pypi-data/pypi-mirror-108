[tool.poetry]
name = "eazy_reverse"
version = "0.1.0"
description = "Reverse(5).print()"
authors = ["Fab4key <fab4key@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
