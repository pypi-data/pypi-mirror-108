from setuptools import setup

setup(name='distributions_awesome_package',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_awesome_package'],
      zip_safe=False)
