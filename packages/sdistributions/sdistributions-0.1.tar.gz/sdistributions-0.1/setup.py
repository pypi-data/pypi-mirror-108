from setuptools import setup

setup(name='sdistributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['sdistributions'],
      author = 'Udacity AWS course',
      zip_safe=False)
