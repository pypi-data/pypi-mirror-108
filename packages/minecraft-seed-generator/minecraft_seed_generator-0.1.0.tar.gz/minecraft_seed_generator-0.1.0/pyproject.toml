[tool.poetry]
name = "minecraft_seed_generator"
version = "0.1.0"
description = "GenerateSeed(5)"
authors = ["Fab4key <fab4key@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
